---
import { CONTACT_LINKS } from '../../shared/constants'
---

<section id="contact" class="relative py-16 sm:py-24 lg:py-32 bg-bg-subtle overflow-hidden">
  <div class="relative z-10 max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-10 sm:mb-16">
      <p class="text-xs font-medium tracking-widest text-text-tertiary mb-3">
        <span class="text-accent">//</span> contact
      </p>
      <h2 class="font-pixel text-base sm:text-lg md:text-2xl text-text-primary mb-4 sm:mb-6">
        <span class="text-accent">&gt;</span> Let's Work Together
      </h2>
      <p class="text-xs sm:text-sm text-text-secondary max-w-3xl">
        Ready to bring your ideas to life? I'd love to hear about your project and discuss how we can work together.
      </p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12">
      <div class="bg-surface border border-border p-5 sm:p-8">
        <h3 class="font-pixel text-[0.55rem] sm:text-xs text-text-primary mb-4 sm:mb-6">
          <span class="text-accent">&gt;</span> send_message
        </h3>

        <form action="https://api.web3forms.com/submit" method="POST" class="space-y-4 sm:space-y-6">
          <input type="hidden" name="access_key" value="c6c37b81-84e5-404d-9332-ed08fefb0e66">
          <div style="position: absolute; left: -9999px;" aria-hidden="true">
            <input type="text" name="botcheck" tabindex="-1" autocomplete="off">
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6">
            <div>
              <label for="firstName" class="block text-[0.65rem] sm:text-xs font-medium text-text-secondary mb-1.5 sm:mb-2">
                <span class="text-accent">&gt;</span> first_name
              </label>
              <input
                type="text"
                id="firstName"
                name="firstName"
                class="w-full px-3 sm:px-4 py-2.5 sm:py-3 bg-bg border border-border text-text-primary placeholder:text-text-tertiary focus:border-accent focus:ring-1 focus:ring-accent focus:outline-none transition-colors text-xs sm:text-sm font-mono"
                required
              />
            </div>
            <div>
              <label for="lastName" class="block text-[0.65rem] sm:text-xs font-medium text-text-secondary mb-1.5 sm:mb-2">
                <span class="text-accent">&gt;</span> last_name
              </label>
              <input
                type="text"
                id="lastName"
                name="lastName"
                class="w-full px-3 sm:px-4 py-2.5 sm:py-3 bg-bg border border-border text-text-primary placeholder:text-text-tertiary focus:border-accent focus:ring-1 focus:ring-accent focus:outline-none transition-colors text-xs sm:text-sm font-mono"
                required
              />
            </div>
          </div>

          <div>
            <label for="email" class="block text-[0.65rem] sm:text-xs font-medium text-text-secondary mb-1.5 sm:mb-2">
              <span class="text-accent">&gt;</span> email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              class="w-full px-3 sm:px-4 py-2.5 sm:py-3 bg-bg border border-border text-text-primary placeholder:text-text-tertiary focus:border-accent focus:ring-1 focus:ring-accent focus:outline-none transition-colors text-xs sm:text-sm font-mono"
              required
            />
          </div>

          <div>
            <label for="subject" class="block text-[0.65rem] sm:text-xs font-medium text-text-secondary mb-1.5 sm:mb-2">
              <span class="text-accent">&gt;</span> subject
            </label>
            <input
              type="text"
              id="subject"
              name="subject"
              class="w-full px-3 sm:px-4 py-2.5 sm:py-3 bg-bg border border-border text-text-primary placeholder:text-text-tertiary focus:border-accent focus:ring-1 focus:ring-accent focus:outline-none transition-colors text-xs sm:text-sm font-mono"
              required
            />
          </div>

          <div>
            <label for="message" class="block text-[0.65rem] sm:text-xs font-medium text-text-secondary mb-1.5 sm:mb-2">
              <span class="text-accent">&gt;</span> message
            </label>
            <textarea
              id="message"
              name="message"
              rows="5"
              class="w-full px-3 sm:px-4 py-2.5 sm:py-3 bg-bg border border-border text-text-primary placeholder:text-text-tertiary focus:border-accent focus:ring-1 focus:ring-accent focus:outline-none transition-colors resize-none text-xs sm:text-sm font-mono"
              required
            ></textarea>
          </div>

          <button
            type="submit"
            class="w-full px-6 py-2.5 sm:py-3 bg-accent text-bg font-medium hover:bg-accent-hover focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 transition-colors uppercase tracking-wider text-xs sm:text-sm"
          >
            [SEND_MESSAGE]
          </button>
        </form>
      </div>

      <div class="space-y-6 sm:space-y-8">
        <div>
          <h3 class="font-pixel text-[0.55rem] sm:text-xs text-text-primary mb-4 sm:mb-6">
            <span class="text-accent">&gt;</span> get_in_touch
          </h3>
          <p class="text-text-secondary text-xs sm:text-sm mb-6 sm:mb-8">
            I'm always open to discussing new opportunities, interesting projects, or just having a chat about technology and development.
          </p>
        </div>

        <div class="space-y-4 border border-border p-4 sm:p-6 bg-surface">
          <div class="flex items-center">
            <span class="text-accent mr-3 text-xs sm:text-sm">$</span>
            <div class="min-w-0">
              <div class="text-[0.65rem] sm:text-xs font-medium text-text-primary">email</div>
              <a href={`mailto:${CONTACT_LINKS.email}`} class="text-accent hover:text-accent-hover transition-colors text-[0.65rem] sm:text-xs break-all">
                {CONTACT_LINKS.email}
              </a>
            </div>
          </div>

          <div class="border-t border-border"></div>

          <div class="flex items-center">
            <span class="text-accent mr-3 text-xs sm:text-sm">$</span>
            <div>
              <div class="text-[0.65rem] sm:text-xs font-medium text-text-primary">location</div>
              <div class="text-text-secondary text-[0.65rem] sm:text-xs">Remote / Philippines</div>
            </div>
          </div>

          <div class="border-t border-border"></div>

          <div class="flex items-center">
            <span class="text-accent mr-3 text-xs sm:text-sm">$</span>
            <div>
              <div class="text-[0.65rem] sm:text-xs font-medium text-text-primary">response_time</div>
              <div class="text-text-secondary text-[0.65rem] sm:text-xs">Usually within 24 hours</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
